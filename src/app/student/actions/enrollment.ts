"use server";

export async function enrollInCourse(courseId: string) {
    // Temporary placeholder
    // Later we will connect real database logic
    return { success: true, message: "Enrollment placeholder working" };
}